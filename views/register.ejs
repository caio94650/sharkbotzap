<!DOCTYPE html>
<html lang="pt-br">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Cadastrar - SharkBotzap</title>
  <style>
    * { box-sizing: border-box; }
    body {
      margin: 0;
      background: #438cc8;
      font-family: 'Segoe UI', sans-serif;
      display: flex;
      align-items: center;
      justify-content: center;
      min-height: 100vh;
      padding: 20px;
    }

    .register-container {
      background: #fff;
      padding: 40px;
      border-radius: 12px;
      width: 100%;
      max-width: 400px;
      box-shadow: 0 8px 16px rgba(0,0,0,0.2);
      text-align: center;
    }

    h2 {
      margin-top: 0;
      color: #438cc8;
    }

    label {
      display: block;
      margin: 20px 0 8px;
      color: #333;
      text-align: left;
    }

    input {
      width: 100%;
      padding: 12px;
      border: 1px solid #ccc;
      border-radius: 8px;
      font-size: 16px;
    }

    button {
      width: 100%;
      padding: 12px;
      margin-top: 30px;
      background-color: #438cc8;
      color: white;
      border: none;
      border-radius: 8px;
      font-size: 16px;
      cursor: pointer;
    }

    button:hover {
      background-color: #356ca4;
    }

    .error, .success {
      margin-top: 20px;
      font-weight: bold;
    }

    .error { color: red; }
    .success { color: green; }

    .login-link {
      display: block;
      margin-top: 20px;
      font-size: 14px;
      color: #438cc8;
    }
  </style>
</head>
<body>
  <div class="register-container">
    <h2>Criar Conta no SharkBotzap</h2>

    <form action="/register" method="POST">
      <label for="name">Nome</label>
      <input type="text" id="name" name="name" required />

      <label for="email">E-mail</label>
      <input type="email" id="email" name="email" required autocomplete="off" />

      <label for="password">Senha</label>
      <input type="password" id="password" name="password" required autocomplete="off" />

      <button type="submit">Cadastrar</button>
    </form>

    <% if (error) { %>
      <div class="error"><%= error %></div>
    <% } %>

    <% if (success) { %>
      <div class="success"><%= success %></div>
    <% } %>

    <a class="login-link" href="/login">🔑 Já tem conta? Entrar</a>
  </div>
</body>
</html>
