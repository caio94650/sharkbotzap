<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Painel - SharkBotzap</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      background: #f4f4f4;
      margin: 0;
      padding: 0;
    }
    header {
      background: #1f6feb;
      color: white;
      padding: 15px;
      text-align: center;
    }
    main {
      max-width: 600px;
      margin: 30px auto;
      background: white;
      padding: 20px;
      border-radius: 8px;
      box-shadow: 0 0 10px rgba(0,0,0,0.1);
    }
    h2 {
      margin-top: 0;
    }
    .status {
      margin-top: 10px;
      padding: 10px;
      border-radius: 5px;
      font-weight: bold;
    }
    .status.conectado {
      background-color: #d4edda;
      color: #155724;
    }
    .status.desconectado {
      background-color: #f8d7da;
      color: #721c24;
    }
    .qr {
      text-align: center;
      margin-top: 20px;
    }
    .qr img {
      width: 250px;
      border: 1px solid #ccc;
      border-radius: 8px;
    }
    .btn {
      display: inline-block;
      padding: 10px 15px;
      background: #1f6feb;
      color: white;
      text-decoration: none;
      border-radius: 5px;
      margin-top: 15px;
    }
    .btn:hover {
      background: #0e4bb8;
    }
  </style>
</head>
<body>
  <header>
    <h1>SharkBotzap</h1>
  </header>
  <main>
    <h2>Bem-vindo, <%= user.name %></h2>
    <p>Email: <strong><%= user.email %></strong></p>

    <div class="status <%= status.status === 'conectado' ? 'conectado' : 'desconectado' %>">
      Status: <%= status.status %>
    </div>

    <% if (qr) { %>
      <div class="qr">
        <p>Escaneie o QR Code abaixo com o WhatsApp:</p>
        <img src="<%= qr %>" alt="QR Code WhatsApp">
      </div>
    <% } else if (status.status !== 'conectado') { %>
      <p>Nenhum QR Code disponível. Clique no botão abaixo para conectar.</p>
    <% } %>

    <% if (status.status !== 'conectado') { %>
      <form action="/dashboard/conectar" method="POST">
        <button class="btn" type="submit">Conectar WhatsApp</button>
      </form>
    <% } else { %>
      <p>✅ Dispositivo já está conectado ao WhatsApp.</p>
    <% } %>

    <form action="/dashboard/aquecedor" method="POST">
      <button class="btn" type="submit">Iniciar Aquecedor</button>
    </form>
  </main>
</body>
</html>
