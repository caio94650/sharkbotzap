<!DOCTYPE html>
<html lang="pt-br">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Dashboard - SharkBotzap</title>
  <style>
    body {
      font-family: 'Segoe UI', sans-serif;
      background: #f0f4f8;
      margin: 0;
      padding: 30px;
    }

    .container {
      max-width: 700px;
      margin: auto;
      background: white;
      border-radius: 10px;
      padding: 40px;
      box-shadow: 0 4px 20px rgba(0,0,0,0.1);
    }

    h1 {
      color: #438cc8;
    }

    .qr-box {
      background: #eef3f8;
      padding: 15px;
      border-radius: 6px;
      margin-top: 20px;
      text-align: center;
    }

    .qr-box img {
      max-width: 300px;
      width: 100%;
      margin-top: 10px;
    }

    .status-box {
      margin-top: 30px;
      background: #dff0d8;
      padding: 15px;
      border-radius: 6px;
      color: #3c763d;
    }

    button {
      margin-top: 20px;
      padding: 10px 20px;
      background-color: #438cc8;
      color: white;
      border: none;
      border-radius: 6px;
      font-size: 16px;
      cursor: pointer;
    }

    button:hover {
      background-color: #356ca4;
    }

    .logout {
      display: inline-block;
      margin-top: 20px;
      background-color: #c0392b;
      padding: 10px 20px;
      color: white;
      border-radius: 6px;
      text-decoration: none;
    }

    .logout:hover {
      background-color: #a93226;
    }
  </style>
</head>
<body>
  <div class="container">
    <h1>Bem-vindo, <%= user.name %>!</h1>
    <p>Email: <%= user.email %></p>

    <% if (qr) { %>
      <div class="qr-box">
        <p><strong>Escaneie o QR Code abaixo com o WhatsApp:</strong></p>
        <img src="<%= qr %>" alt="QR Code WhatsApp" />
      </div>
    <% } else { %>
      <p>QR Code ainda não gerado. <form style="display:inline;" method="POST" action="/dashboard/conectar"><button type="submit">Conectar WhatsApp</button></form></p>
    <% } %>

    <div class="status-box">
      <strong>Status:</strong> <%= status.status %><br>
      <strong>Última Mensagem:</strong> <%= status.lastMessage %>
    </div>

    <% if (qr) { %>
    <form action="/dashboard/aquecedor" method="POST">
      <button type="submit">Iniciar Aquecedor</button>
    </form>
    <% } else { %>
    <p style="margin-top: 20px; color: #888;">Conecte-se ao WhatsApp para iniciar o aquecedor.</p>
    <% } %>

    <a class="logout" href="/logout">Sair</a>
  </div>
</body>
</html>
