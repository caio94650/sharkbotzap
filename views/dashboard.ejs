<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>SharkBotzap - Painel</title>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700&display=swap" rel="stylesheet">
  <style>
    * {
      box-sizing: border-box;
      font-family: 'Inter', sans-serif;
    }
    body {
      margin: 0;
      padding: 0;
      background: #f2f3f5;
      color: #333;
    }
    header {
      background: #1e6fff;
      color: white;
      padding: 20px;
      text-align: center;
      font-size: 1.6rem;
      font-weight: 700;
    }
    main {
      max-width: 500px;
      margin: 40px auto;
      background: white;
      padding: 30px;
      border-radius: 12px;
      box-shadow: 0 4px 20px rgba(0,0,0,0.08);
    }
    h2 {
      margin-top: 0;
      font-size: 1.4rem;
      font-weight: 600;
    }
    .info p {
      margin: 8px 0;
    }
    .status {
      margin: 20px 0;
      padding: 12px;
      border-radius: 6px;
      font-weight: 600;
      text-align: center;
    }
    .conectado {
      background-color: #e6ffed;
      color: #2e7d32;
    }
    .desconectado {
      background-color: #ffe4e6;
      color: #c62828;
    }
    .qr {
      text-align: center;
      margin: 20px 0;
    }
    .qr img {
      width: 240px;
      border-radius: 8px;
      border: 1px solid #ccc;
    }
    .btn {
      display: block;
      width: 100%;
      background: #1e6fff;
      color: white;
      border: none;
      border-radius: 6px;
      padding: 12px;
      margin: 10px 0;
      font-weight: 600;
      cursor: pointer;
      transition: background 0.3s;
    }
    .btn:hover {
      background: #125ce0;
    }
    .footer {
      text-align: center;
      font-size: 0.85rem;
      color: #888;
      margin-top: 40px;
    }
  </style>
  <script>
    setInterval(() => {
      fetch('/dashboard')
        .then(res => res.text())
        .then(html => {
          const parser = new DOMParser();
          const doc = parser.parseFromString(html, 'text/html');
          const statusDiv = doc.querySelector('.status');
          const qrImg = doc.querySelector('.qr img');
          if (statusDiv && qrImg) {
            document.querySelector('.status').outerHTML = statusDiv.outerHTML;
            document.querySelector('.qr img').src = qrImg.src;
          }
        });
    }, 6000);
  </script>
</head>
<body>
  <header>SharkBotzap</header>
  <main>
    <h2>Bem-vindo, <%= user.name %></h2>
    <div class="info">
      <p>Email: <strong><%= user.email %></strong></p>
    </div>

    <div class="status <%= status.status === 'conectado' ? 'conectado' : 'desconectado' %>">
      Status: <%= status.status %>
    </div>

    <% if (qr) { %>
      <div class="qr">
        <p>Escaneie o QR Code abaixo com o WhatsApp:</p>
        <img src="<%= qr %>" alt="QR Code WhatsApp">
      </div>
    <% } else if (status.status !== 'conectado') { %>
      <p style="text-align: center; color: #999;">Nenhum QR Code disponível no momento.</p>
    <% } %>

    <% if (status.status !== 'conectado') { %>
      <form action="/dashboard/conectar" method="POST">
        <button class="btn" type="submit">Conectar WhatsApp</button>
      </form>
    <% } else { %>
      <p style="text-align: center; color: #4caf50;">✅ Dispositivo conectado com sucesso.</p>
    <% } %>

    <form action="/dashboard/aquecedor" method="POST">
      <button class="btn" type="submit">Iniciar Aquecedor</button>
    </form>

    <form action="/dashboard/debug" method="POST">
      <button class="btn" type="submit">Testar POST (Debug)</button>
    </form>
  </main>
  <div class="footer">
    &copy; 2025 SharkBotzap. Todos os direitos reservados.
  </div>
</body>
</html>
