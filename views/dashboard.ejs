<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>SharkBotzap</title>
  <link rel="icon" href="/favicon.ico" />
  <style>
    * {
      box-sizing: border-box;
    }

    body {
      margin: 0;
      font-family: 'Segoe UI', sans-serif;
      background: #f4f6f8;
      color: #333;
    }

    header {
      background: #1f6feb;
      color: #fff;
      padding: 20px;
      text-align: center;
      font-size: 24px;
      font-weight: bold;
      box-shadow: 0 2px 6px rgba(0,0,0,0.15);
    }

    main {
      max-width: 500px;
      margin: 40px auto;
      background: #fff;
      padding: 30px 25px;
      border-radius: 12px;
      box-shadow: 0 4px 10px rgba(0,0,0,0.1);
    }

    .user-info {
      margin-bottom: 25px;
    }

    .user-info strong {
      color: #000;
    }

    .status-box {
      padding: 12px;
      border-radius: 6px;
      font-weight: bold;
      margin-bottom: 20px;
      display: flex;
      align-items: center;
      gap: 10px;
    }

    .status-box.conectado {
      background: #d1fae5;
      color: #065f46;
    }

    .status-box.desconhecido {
      background: #fee2e2;
      color: #991b1b;
    }

    .qr-container {
      text-align: center;
      margin-bottom: 20px;
    }

    .qr-container img {
      width: 220px;
      border-radius: 8px;
      border: 1px solid #ddd;
    }

    .btn {
      display: block;
      width: 100%;
      margin: 10px 0;
      padding: 12px;
      background: #1f6feb;
      color: white;
      border: none;
      border-radius: 6px;
      font-size: 16px;
      cursor: pointer;
      transition: background 0.3s;
    }

    .btn:hover {
      background: #0f4dc5;
    }

    .info {
      font-size: 14px;
      text-align: center;
      color: #555;
      margin-top: 25px;
    }

    @media (max-width: 500px) {
      main {
        margin: 20px;
        padding: 20px;
      }

      header {
        font-size: 20px;
      }
    }
  </style>
</head>
<body>
  <header>SharkBotzap</header>

  <main>
    <div class="user-info">
      <p><strong>Bem-vindo,</strong> <%= user.name %></p>
      <p>Email: <strong><%= user.email %></strong></p>
    </div>

    <div class="status-box <%= status.status === 'conectado' ? 'conectado' : 'desconhecido' %>">
      <%= status.status === 'conectado' ? '🟢 Conectado ao WhatsApp' : '🔴 Desconectado' %>
    </div>

    <% if (qr) { %>
      <div class="qr-container">
        <p>Escaneie o QR Code abaixo com o WhatsApp:</p>
        <img src="<%= qr %>" alt="QR Code do WhatsApp" />
      </div>
    <% } else if (status.status !== 'conectado') { %>
      <p style="text-align: center;">Nenhum QR Code disponível. Clique no botão abaixo para conectar.</p>
    <% } %>

    <% if (status.status !== 'conectado') { %>
      <form action="/dashboard/conectar" method="POST">
        <button class="btn">Conectar WhatsApp</button>
      </form>
    <% } %>

    <form action="/dashboard/aquecedor" method="POST">
      <button class="btn">Iniciar Aquecedor</button>
    </form>

    <form action="/dashboard/debug" method="POST">
      <button class="btn">Testar POST (Debug)</button>
    </form>

    <div class="info">Painel do cliente SharkBotzap • Versão beta</div>
  </main>
</body>
</html>
