<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title><%= title || "SharkBotzap" %></title>
  <link rel="icon" href="https://i.ibb.co/3ydtSJNq/logo.png" type="image/png">
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600&display=swap" rel="stylesheet">
  <style>
    * { box-sizing: border-box; }

    body {
      margin: 0;
      font-family: 'Inter', sans-serif;
      background: linear-gradient(135deg, #2563eb, #1d4ed8);
      color: #111827;
      min-height: 100vh;
    }

    .container {
      display: flex;
      justify-content: center;
      padding: 2rem 1rem;
    }

    .card {
      background-color: white;
      padding: 2rem;
      border-radius: 16px;
      box-shadow: 0 12px 24px rgba(0, 0, 0, 0.15);
      width: 100%;
      max-width: 460px;
      text-align: center;
      animation: fadeIn 0.6s ease;
    }

    @keyframes fadeIn {
      from { opacity: 0; transform: translateY(-20px); }
      to { opacity: 1; transform: translateY(0); }
    }

    .logo {
      height: 60px;
      margin-bottom: 1.2rem;
    }

    h2 {
      margin: 0 0 1rem;
      color: #1f2937;
    }

    .info p {
      margin: 0.5rem 0;
    }

    .status {
      margin-top: 0.5rem;
      font-weight: 600;
    }

    .status.conectado { color: #10b981; }
    .status.desconectado, .status.desconhecido { color: #ef4444; }

    .qr {
      margin-top: 1rem;
    }

    .qr img {
      margin-top: 0.5rem;
      max-width: 220px;
      border-radius: 8px;
    }

    .btn, .btn-red {
      display: block;
      background-color: #2563eb;
      color: white;
      border: none;
      padding: 0.75rem;
      width: 100%;
      font-size: 1rem;
      border-radius: 8px;
      margin-top: 1rem;
      cursor: pointer;
      transition: background 0.2s ease-in-out;
    }

    .btn:hover { background-color: #1e40af; }
    .btn-red { background-color: #ef4444; }
    .btn-red:hover { background-color: #b91c1c; }

    .instrucoes {
      margin-top: 2rem;
      background-color: #f9fafb;
      border-radius: 12px;
      padding: 1.5rem;
      text-align: left;
    }

    .instrucoes h3 {
      margin-top: 0;
      margin-bottom: 0.8rem;
      color: #1f2937;
    }

    .instrucoes ul {
      padding-left: 1rem;
      margin: 0;
    }
  </style>
</head>
<body>
