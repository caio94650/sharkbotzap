<!DOCTYPE html>
<html lang="pt-br">
<head>
  <meta charset="UTF-8" />
  <title>SharkBotzap - Login</title>
  <style>
    body {
      font-family: 'Segoe UI', sans-serif;
      background: #438cc8;
      margin: 0;
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
      animation: fadeIn 0.8s ease-in;
    }

    @keyframes fadeIn {
      from { opacity: 0; }
      to { opacity: 1; }
    }

    .login-container {
      background: #fff;
      padding: 40px;
      border-radius: 12px;
      width: 100%;
      max-width: 400px;
      box-shadow: 0 0 20px rgba(0, 0, 0, 0.2);
      text-align: center;
      animation: fadeIn 1s ease;
    }

    .login-container img {
      width: 120px;
      margin-bottom: 15px;
    }

    h2 {
      margin-bottom: 20px;
      color: #222;
    }

    input {
      width: 100%;
      padding: 12px;
      margin-bottom: 15px;
      border: 1px solid #ccc;
      border-radius: 8px;
      font-size: 16px;
    }

    .password-wrapper {
      position: relative;
    }

    .password-wrapper input {
      padding-right: 40px;
    }

    .toggle-password {
      position: absolute;
      top: 50%;
      right: 12px;
      transform: translateY(-50%);
      cursor: pointer;
      color: #666;
    }

    button {
      width: 100%;
      padding: 12px;
      background: #438cc8;
      border: none;
      color: white;
      font-weight: bold;
      border-radius: 8px;
      font-size: 16px;
      cursor: pointer;
      transition: 0.3s;
    }

    button:hover {
      background: #356ca4;
    }

    .link {
      display: block;
      margin-top: 15px;
      color: #555;
    }

    .error {
      background: #f44336;
      color: white;
      padding: 10px;
      border-radius: 6px;
      margin-bottom: 15px;
    }

    .success {
      background: #4caf50;
      color: white;
      padding: 10px;
      border-radius: 6px;
      margin-bottom: 15px;
    }
  </style>
</head>
<body>
  <div class="login-container">
    <img src="/logo.png" alt="Logo SharkBotzap">
    <h2>SharkBotzap</h2>

    <% if (typeof error !== 'undefined' && error) { %>
      <div class="error"><%= error %></div>
    <% } %>

    <% if (typeof success !== 'undefined' && success) { %>
      <div class="success"><%= success %></div>
    <% } %>

    <form action="/login" method="POST">
      <input type="email" name="email" id="email" placeholder="E-mail" required autofocus />
      
      <div class="password-wrapper">
        <input type="password" name="password" id="password" placeholder="Senha" required />
        <span class="toggle-password" onclick="togglePassword()">👁️</span>
      </div>
      
      <button type="submit">Entrar</button>
    </form>

    <a class="link" href="/register">Criar nova conta</a>
  </div>

  <script>
    function togglePassword() {
      const pass = document.getElementById("password");
      pass.type = pass.type === "password" ? "text" : "password";
    }

    document.getElementById("email").focus();
  </script>
</body>
</html>